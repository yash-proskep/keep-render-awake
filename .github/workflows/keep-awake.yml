name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"  # Every 10 minutes UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your Render app
        run: curl https://label-studio-on-render-x49n.onrender.com/
